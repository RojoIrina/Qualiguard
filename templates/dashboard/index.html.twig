<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"><title>QualiGuard | Decision Center</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #0b0f1a; }
        .glass { background: rgba(255, 255, 255, 0.02); backdrop-filter: blur(15px); border: 1px solid rgba(255, 255, 255, 0.05); }
        input[type=range] { height: 6px; -webkit-appearance: none; background: #1e293b; border-radius: 5px; }
    </style>
</head>
<body class="text-slate-200 antialiased font-sans">

    <nav class="glass sticky top-0 px-10 py-5 flex justify-between items-center z-50">
        <span class="text-2xl font-black bg-gradient-to-r from-blue-400 to-emerald-400 bg-clip-text text-transparent italic">QUALIGUARD</span>
        <div class="flex items-center gap-6">
            <span class="text-[10px] font-mono text-slate-500 uppercase tracking-widest italic">ISO 25010 Monitoring</span>
            <a href="{{ path('app_logout') }}" class="text-slate-500 hover:text-red-400 transition"><i class="fa-solid fa-power-off"></i></a>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto p-10 grid grid-cols-12 gap-10">
        
        <aside class="col-span-12 lg:col-span-4 glass rounded-[3rem] p-8">
            <h2 class="text-xl font-bold mb-8 italic flex items-center gap-3"><i class="fa-solid fa-database text-blue-400"></i> Data Input</h2>
            <form action="{{ path('app_dashboard') }}" method="GET" class="space-y-8">
                <div>
                    <label class="text-[10px] font-bold text-slate-500 uppercase block mb-3">Taux Bugs ({{ bugs }}/kLoC)</label>
                    <input type="range" name="bugs" min="0" max="10" step="0.1" value="{{ bugs }}" oninput="this.form.submit()" class="w-full accent-blue-500">
                </div>
                <div>
                    <label class="text-[10px] font-bold text-slate-500 uppercase block mb-3">Dette Tech ({{ dette }}%)</label>
                    <input type="range" name="dette" min="0" max="40" value="{{ dette }}" oninput="this.form.submit()" class="w-full accent-red-500">
                </div>
                <div>
                    <label class="text-[10px] font-bold text-slate-500 uppercase block mb-3">Tests Coverage ({{ coverage }}%)</label>
                    <input type="range" name="coverage" min="0" max="100" value="{{ coverage }}" oninput="this.form.submit()" class="w-full accent-emerald-500">
                </div>
                <div>
                    <label class="text-[10px] font-bold text-slate-500 uppercase block mb-3">Lead Time ({{ leadTime }}j)</label>
                    <input type="range" name="leadtime" min="1" max="20" value="{{ leadTime }}" oninput="this.form.submit()" class="w-full accent-purple-500">
                </div>
                <div>
                    <label class="text-[10px] font-bold text-slate-500 uppercase block mb-3">NPS Score ({{ nps }})</label>
                    <input type="range" name="nps" min="-100" max="100" value="{{ nps }}" oninput="this.form.submit()" class="w-full accent-orange-500">
                </div>
            </form>
        </aside>

        <section class="col-span-12 lg:col-span-8 space-y-8">
            <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
                <div class="glass p-4 rounded-2xl border-t-2 border-blue-500 text-center">
                    <p class="text-[9px] font-bold text-slate-500 uppercase">Qualité</p>
                    <p class="text-xl font-black mt-1">{{ kpiQuality|number_format(0) }}%</p>
                </div>
                <div class="glass p-4 rounded-2xl border-t-2 border-red-500 text-center">
                    <p class="text-[9px] font-bold text-slate-500 uppercase">Maintenabilité</p>
                    <p class="text-xl font-black mt-1">{{ kpiSecurity|number_format(0) }}%</p>
                </div>
                <div class="glass p-4 rounded-2xl border-t-2 border-emerald-500 text-center">
                    <p class="text-[9px] font-bold text-slate-500 uppercase">Robustesse</p>
                    <p class="text-xl font-black mt-1">{{ kpiRobustness|number_format(0) }}%</p>
                </div>
                <div class="glass p-4 rounded-2xl border-t-2 border-purple-500 text-center">
                    <p class="text-[9px] font-bold text-slate-500 uppercase">Vitesse</p>
                    <p class="text-xl font-black mt-1">{{ kpiProcess|number_format(0) }}%</p>
                </div>
                <div class="glass p-4 rounded-2xl border-t-2 border-orange-500 text-center">
                    <p class="text-[9px] font-bold text-slate-500 uppercase">Usage</p>
                    <p class="text-xl font-black mt-1">{{ kpiUsage|number_format(0) }}%</p>
                </div>
            </div>

            <div class="bg-gradient-to-br from-{{ status }}-600/20 to-transparent border border-{{ status }}-500/20 p-10 rounded-[3rem] text-center shadow-2xl">
                <p class="text-slate-500 font-bold uppercase tracking-[0.3em] text-xs mb-4 italic">Global Health Index</p>
                <div class="text-8xl font-black text-{{ status }}-400 mb-6 tracking-tighter">
                    {{ globalScore|number_format(0) }}%
                </div>
                <h3 class="text-2xl font-bold uppercase text-white mb-4 underline decoration-{{ status }}-500 decoration-4 underline-offset-8">{{ label }}</h3>
                <p class="text-lg text-slate-400 font-light max-w-lg mx-auto leading-relaxed">
                    {{ proposition }}
                </p>
            </div>
        </section>
    </main>
</body>
</html>